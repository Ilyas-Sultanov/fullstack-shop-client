import { FC } from "react";
import NavBar from "../NavBar/NavBar";

const Header: FC = () => {
    return (
        <header className="header">
            <NavBar />
        </header>
    )
}

export default Header;
